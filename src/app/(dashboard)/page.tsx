import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard | Tripio",
  description: "...",
};

export default function DashboardHomePage() {
  return (
    <div>
      <main>Dashboard Home</main>
    </div>
  );
}
